{% extends 'ZikiMusicBundle::layout.html.twig' %}

{% block content %}
<h1>Track creation</h1>

<form action="{{ path('track_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('track') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
